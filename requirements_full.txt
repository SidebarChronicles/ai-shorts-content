# Full requirements for all pipeline scripts
# Install into .venv-upload: pip install -r requirements_full.txt

# Already in requirements_upload.txt (Google/YouTube API)
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.1.0

# ElevenLabs TTS (make_audio_elevenlabs.py)
requests>=2.31.0

# YouTube trailer fetcher fallback (fetch_trailer.py)
yt-dlp>=2024.1.0

# Claude API analytics suggestions (suggest_improvements.py, game_script_writer.py)
# Need >=0.40 for cache_control on system blocks (the writer-prompt caching pattern)
anthropic>=0.40.0

# Smoke tests (pure helpers — no API mocks)
pytest>=7.0.0

# Tier 3.3: YOLOv8 smart-crop for trailers (smart_crop_yolo.py)
# ~2GB total install with model weights. Only loaded when --crop smart_crop
# is passed to render_game_video.py; default pillarbox path still works
# without these installed.
ultralytics>=8.1.0
opencv-python>=4.9.0
